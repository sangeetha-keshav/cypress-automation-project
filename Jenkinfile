pipeline{
    agent any
    stages {
        stage('build') {
            steps {
                echo 'building project'
                sh 'npm install'
            }
        }
        stage('cypress test') {
            steps {
                echo 'running cypress automation test'
                sh 'npx cypress run'
            }
        }
    }
}
