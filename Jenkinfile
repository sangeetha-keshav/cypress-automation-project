pipeline{
    agent any
    tools {nodejs "node"}
    stages {
        stage('build and cypress test') {
            steps {
                echo 'npm install and running cypress automation test'
                sh '''
                    npm install
                    npx cypress run
                '''
            }
        }
    }
}
